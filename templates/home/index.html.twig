{% extends 'base.html.twig' %}


{% block body %}

	<div class="container">
		<ul class="nav flex-column">
			{% for link in linksCategory %}
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href=" {{ path('app_category', {'id':link.id}) }} ">
						{{link.name}}
					</a>
				</li>
			{% endfor %}
		</ul>

	</div>
{% endblock %}
